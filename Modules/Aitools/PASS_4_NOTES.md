# Pass 4 (Floating Widget Upgrade)

- Added tool browser/picker UI in floating widget.
- Fetches registered tools from GET /account/aitools/tools.
- Adds quick action chips (Today, Unpaid, Find client, Draft task).
- Adds safer client-side rendering (HTML escaped, simple inline code).
- Bumped module version to 1.2.0.
