<div class="d-flex flex-wrap gap-2 mt-2">
    <span class="badge bg-secondary">{{ __('bookingmodule::dispatch.legend.pending') }}</span>
    <span class="badge bg-success">{{ __('bookingmodule::dispatch.legend.approved') }}</span>
    <span class="badge bg-danger">{{ __('bookingmodule::dispatch.legend.cancelled') }}</span>
</div>
